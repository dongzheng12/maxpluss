# Local vitest PostgreSQL test DB template.
# tests/globalSetup.ts and tests/setup.ts load this when DATABASE_URL and .env.test are absent.
#
# Start the local test DB when needed:
# docker run -d --name bxz-pg-test \
#   -e POSTGRES_DB=biaozhunxiaozhi_test \
#   -e POSTGRES_USER=test \
#   -e POSTGRES_PASSWORD=test \
#   -p 55432:5432 \
#   postgres:15
DATABASE_URL="postgresql://test:test@localhost:55432/biaozhunxiaozhi_test"
